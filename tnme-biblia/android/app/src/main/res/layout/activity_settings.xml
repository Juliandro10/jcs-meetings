<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/tnme_bg"
    android:orientation="vertical">

    <include layout="@layout/view_tnme_top_bar" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/settings_bible_heading"
                android:textColor="@color/tnme_text"
                android:textSize="15sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/settingsStatus"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:textColor="@color/tnme_muted"
                android:textSize="14sp" />

            <TextView
                android:id="@+id/settingsHelp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:lineSpacingExtra="4dp"
                android:text="@string/settings_help"
                android:textColor="@color/tnme_muted"
                android:textSize="14sp"
                android:visibility="gone" />

            <Button
                android:id="@+id/settingsPickButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:background="@color/tnme_accent_btn"
                android:padding="14dp"
                android:text="@string/pick_jwpub"
                android:textColor="@color/tnme_white" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>
